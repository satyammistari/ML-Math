"""ui package — terminal UI components for mlmath."""
from ui.colors import *
from ui.widgets import (box, table, panel, sparkline, print_sparkline,
                        bar_chart, section_header, breadcrumb, nav_bar,
                        pager, code_block, title_banner, hr)
from ui.menu import (main_menu, block_menu, topic_nav,
                     mark_visited, mark_completed, get_progress,
                     search_topics, clear)
